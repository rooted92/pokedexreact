import React from 'react';
import { GetPokemonByNameOrId } from '../services/data';

interface PokemonItemProps {
    pokemonName: string;
}

const PokemonItem = ({pokemonName} : PokemonItemProps) => {

    return (
        <>
            
        </>
    )
}